* color the edges with the same principle as nodes using the number of
  calls

* see if it is possible to separate the traces (maybe using the ID)

* options to specify the desired output

* write tests
